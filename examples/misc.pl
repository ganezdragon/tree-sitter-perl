# ellipsis
{ ... }
sub foo { ... }
...;
sub somemeth {
  my $self = shift;
  ...;
}


# special scalar variable
$;;

$~;

$_;

$a;
$b;

# scalar variable
my $ab;

# some complex structures
"$sdfs"->sdf();
q{sdf $dsf sd};
print q{sdfsd{}};

my @fsdf = split / DF /g, 'abcdadad';
split qr/dsf/, 'sdf';

/(.*)($startdate)/;
split /$sfsd/, 'sdf';

s/sdf/$badrcgreetingquestionmarkstring/g;
s/sf/sf/g;

/sdfsd/g;
# print Dumper $arg;

map { $_ } @adf;

print map { $_ } @adf;

"$sdfs"->sdf();
q{sdf $dsf sd};
print q{sdfsd{}};