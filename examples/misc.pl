# ellipsis
{ ... }
sub foo { ... }
...;
sub somemeth {
  my $self = shift;
  ...;
}


# special scalar variable
$;;

$~;

$_;

$a;
$b;

# scalar variable
my $ab;

# some complex structures
"$sdfs"->sdf();
q{sdf $dsf sd};
print q{sdfsd{}};

my @fsdf = split / DF /g, 'abcdadad';
split qr/dsf/, 'sdf';

/(.*)($startdate)/;
split /$sfsd/, 'sdf';

s/sdf/$badrcgreetingquestionmarkstring/g;
s/sf/sf/g;

/sdfsd/g;
# print Dumper $arg;

map { $_ } @adf;

print map { $_ } @adf;

"$sdfs"->sdf();
q{sdf $dsf sd};
print q{sdfsd{}};

$sdfds = eval {
    print "dfs";
};

my $args->{FAMILY} = 1;

my $isdst = (localtime($secondsfromepoch))[8];

my ($notedbh, $df) = 1;

my $dsfsd = $fsdf;

my ($dsfs{dsfs}, $dsf) = 1;

local $ENV{STATSDGROUP} = 'DEV';

sort $somesub keys %$hashgroup;


$dsf = try {
  print "hello";
};

map
{
  $_*2
} (1,2);


scalar(Tool->select()->sfds());
scalar Tool->select()->sfds();

print Dumper $args;

map { $_; }@adfa;
map { $_; } @adfa;

$args = "sdfs";
print$args;


sfs::sdfs::sfec($dfd)->sdfs();

$args -> {SETTING} = '';

$set->{sub};

for my $i (0 .. $#{ $args->{something} }) {
}

$#messages;


&_privateFunction($arguments);

(SomePackage->new())[0]->{SOMEID};

sub printArgs {
    print Dumper(@_);
}
# printArgs (1,2), "meow";
# printArgs "meow", (1,2);
printArgs 1, 2;
printArgs;
printArgs(12,3,2);
printArgs()->sdfs();


$somehash{"value" . $value} .= "";

$SomePackage::SubPackage{value};